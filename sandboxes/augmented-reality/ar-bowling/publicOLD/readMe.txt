-- Changed the floor to simulate a bowling lane, added 4 skittles and changed the player from a Cube to a Ball

-- Changed jump to bowl to simulate bowling

-- Added Hammer.js. Only have tap available at the moment but will change to swipe when all is working

-- Adds velocity on tap that sends the ball straight towards the skittles (Static at the moment, will need to turn into a variable that is passed through)

-- Added Hammer.js swipe functionality to direct the ball towards the skittles (adding screen-size scaling and velocity scaling next version)