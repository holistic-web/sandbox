0-03:

-- https://github.com/ngokevin/aframe-component-boilerplate/blob/master/index.js
    ^ seems like a useful resource for further expansion
-- possibly reskin m-AR.io as augmented reality geometry dash?
-- move jump commands onto the jump component so they can easily be added to multiple elements

> axis updates are from https://www.npmjs.com/package/aframe-physics-system
> axis-es's are now locked
    ~ couldn't figure out hinges 
    ~ jump impulse direction now relative to ground rather than player

-- axis still need work as the bounceback force is not locked in direction and so can cause tipping